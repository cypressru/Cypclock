# FILEPATH: gui.py

import tkinter as tk

# Create the main window
root = tk.Tk()
root.title("Under Construction")

# Create a label with the text "Under construction"
label = tk.Label(root, text="Under construction")
label.pack(pady=50)

# Run the main loop
root.mainloop()
